//c program to read an input key stroke (alphabet, numeric or special symbol) and output its equivalent ASCII code
void main()
{
	char ch;
	//int cont;
	//do{
	printf("\nenter any key from keyboard(alphabet,numeric or special symbol) : ");//try for any key enterd from keyboard
	scanf("%c",&ch);
	printf("ASCII code of the key entered by you is : %d",ch);
	/*printf("\nDo you want to continue ,press 0 for NO and 1 for YES : ");
	scanf("%d",&cont);
	}while(cont==1);*/
}
